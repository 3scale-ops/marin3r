
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: envoydeployments.operator.marin3r.3scale.net
spec:
  group: operator.marin3r.3scale.net
  names:
    kind: EnvoyDeployment
    listKind: EnvoyDeploymentList
    plural: envoydeployments
    singular: envoydeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EnvoyDeployment is the Schema for the envoydeployments API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EnvoyDeploymentSpec defines the desired state of EnvoyDeployment
            properties:
              clusterID:
                description: Defines the local service cluster name where Envoy is
                  running. Defaults to the NodeID in the EnvoyConfig if unset
                type: string
              discoveryServiceRef:
                description: DiscoveryServiceRef points to a DiscoveryService in the
                  same namespace
                type: string
              duration:
                description: Defines the duration of the client certificate that is
                  used to authenticate with the DiscoveryService
                type: string
              envoyConfigRef:
                description: EnvoyConfigRef points to an EnvoyConfig in the same namespace
                  that holds the envoy resources for this Deployment
                type: string
              extraArgs:
                description: Allows the user to define extra command line arguments
                  for the Envoy process
                items:
                  type: string
                type: array
              image:
                description: Image is the envoy image and tag to use
                type: string
              ports:
                description: 'Ports exposed by the Envoy container TODO: calculate
                  this inspecting the list of listeners in the published EnvoyConfigRevision'
                items:
                  description: ContainerPort defines port for the Marin3r sidecar
                    container
                  properties:
                    name:
                      description: Port name
                      type: string
                    port:
                      description: Port value
                      format: int32
                      type: integer
                    protocol:
                      default: TCP
                      description: Protocol. Defaults to TCP.
                      type: string
                  required:
                  - name
                  - port
                  type: object
                type: array
              resources:
                description: Resources holds the resource requirements to use for
                  the Envoy Deployment. When not set it defaults to no resource requests
                  nor limits.
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources
                      allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute
                      resources required. If Requests is omitted for a container,
                      it defaults to Limits if that is explicitly specified, otherwise
                      to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                    type: object
                type: object
            required:
            - discoveryServiceRef
            - envoyConfigRef
            type: object
          status:
            description: EnvoyDeploymentStatus defines the observed state of EnvoyDeployment
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
